package org.cdac;

public enum Menu {

	PUBLIC(1),
	PRIVATE(2),
	DEFAULT(3),
	EXIT_1(4),
	ABSTRACT(1);
	
	private int choice;
	
	private Menu(int choice) {
		this.choice = choice;
	}
	
	
	
}
